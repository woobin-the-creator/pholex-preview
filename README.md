# pholex-preview

Pholex UI 시안 비교용 **상시** 정적 프리뷰. 내용은 진행 중인 작업에 따라 매번 루트째 교체된다.

- 데이터는 전부 합성 데모 픽스처다 (실제 사번·이름·부서·lot 아님).
- 백엔드 없이 도는 정적 빌드이므로 일부 API 호출은 404가 난다 — 의도된 상태다.
- 시안 선택이 끝나면 루트를 이 플레이스홀더로 되돌린다(공개 자산 방치 방지).
